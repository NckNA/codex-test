# TEST-PR-004: GH_TOKEN Direct Delivery Check

This PR checks the ability of the Jules session to use `GH_TOKEN` directly via the GitHub CLI (`gh`) to create a Pull Request to `main`.

- No application code was changed.
- Created via terminal commands, bypassing the built-in submit flow.
